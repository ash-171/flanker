# Tox (https://tox.wiki/) runs the test suite across supported Python versions.
# "pip install tox" and then run "tox" from this directory.

[tox]
envlist = py39, py310, py311, py312, py313
isolated_build = true

[testenv]
extras =
    tests
    validator
commands = pytest {posargs}
